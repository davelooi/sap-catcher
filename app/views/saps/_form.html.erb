<%= form_with(model: sap, local: true) do |form| %>
  <% if sap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sap.errors.count, "error") %> prohibited this sap from being saved:</h2>

      <ul>
      <% sap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
